defmodule EmryUiWeb.Components.ButtonGroup do
  @moduledoc """
  Button groups combine sets of buttons into toolbars or split buttons for
  more complex components. Button groups are also useful for acting as mini
  “tabs” in UI, for example, switching between date ranges.
  """
  use Surface.Component

  def render(assigns) do
    ~F"""
    <div></div>
    """
  end
end